'use strict' /*Сука строгий режим.*/
let abort; /*Создание неопределенной переменной.*/
abort = 'ГРЕХ'; /*Присваивание переменной значение. */
const grex = 'ГРЕХ'; /*Создаём константу.*/
abort = grex; /*Присваиваем значение константы переменной "abort"*/